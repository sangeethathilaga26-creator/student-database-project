# Student Database Management System

A C-based application designed to manage student records efficiently. This project allows users to perform core database operations through a console interface.

## 🚀 Features
* **Add Students:** Register new student details into the system.
* **View Records:** Display a list of all enrolled students.
* **Search:** Quickly find a student by their ID or Name.
* **Delete:** Remove student records from the database.
* **Data Persistence:** Records are saved to a `.txt` file so data isn't lost when the program closes.
